#include <stdio.h>

int main() {
    int iSize = 10;
    for (int i=1;i<=iSize;i++) {
        printf("%2d: ",i);
        for (int j=1;j<=i;j++) {
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}
